services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: discord-bot:latest
    container_name: discord-bot
    restart: unless-stopped
    network_mode: host
    volumes:
       - $DISK/discord-bot/:/app/discord-bot/config/:Z